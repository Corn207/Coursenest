// import { useParams } from "react-router";

export default function Course() {
    // const { id } = useParams();
    // console.log(id);

    return (
        <div>
            <p>course</p>
        </div>
    );
}
